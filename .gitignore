# google cloud service account settings
the-slide-machine-6d173e88efd3.json

# Dependencies & build output
node_modules/
dist/
coverage/

# Environment files — .env.example files are committed, real env files never
.env
.env.*
!.env.example
*.local

# Test artifacts
test-results/
playwright-report/
blob-report/

# OS
.DS_Store

# Legacy V1 secret file (defense in depth)
openai.key

# Kept locally, excluded from the repo (also purged from history)
docs/GRANT_PROPOSAL.md
docs/LAB.md
docs/grant_proposal.pdf

# Local upload storage (STORAGE_PROVIDER=local)
.uploads*/

# Scratch space for throwaway probe scripts — local only, never committed
server/__p/

# Mail the e2e run captures to read verification / reset links (AUTH-3/AUTH-4)
e2e/.mail-e2e.log
