/**
 * "Connecting an AI assistant". Like About, the words live in
 * `@slide-machine/shared`; it names no operator, so this is a plain
 * re-export.
 */
export { ASSISTANTS } from '@slide-machine/shared'
