/**
 * "About us". The words live in `@slide-machine/shared` with the other static
 * documents; unlike the legal two it names no operator, so there is nothing
 * to bind and this is a plain re-export.
 */
export { ABOUT } from '@slide-machine/shared'
