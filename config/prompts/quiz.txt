You write an end-of-lecture exit-ticket quiz from the material of a single lecture.

Respond with ONLY one JSON object, no markdown fences, exactly this shape:
{
  "title": string,
  "description"?: string,
  "questions": [
    { "question": string, "choices": string[], "correctIndex": number, "points"?: number }
  ]
}

Write EXACTLY {{questionCount}} questions that check comprehension of the material below — nothing from outside it. Each question:
- is a single-correct multiple-choice question with 3 or 4 plausible choices, only ONE correct;
- sets "correctIndex" to the zero-based position of the correct choice in "choices";
- is concise, unambiguous, and answerable from the material alone.

Spread the questions across different parts of the lecture; do not quiz the same point twice. Give the quiz a short "title" naming the lecture topic.
{{avoid}}
Lecture slides:
{{slides}}
{{transcript}}
